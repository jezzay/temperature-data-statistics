{
  "name": "temperature-data-statistics",
  "version": "1.0.0",
  "description": "generates statistics from temperature data",
  "main": "./bin/temperature-data-statistics.js",
  "scripts": {
    "start": "node ./bin/temperature-data-statistics.js",
    "build": "tsc",
    "watch": "tsc -w --listEmittedFiles",
    "test": "jest --watch --notify",
    "prepublish": "npm run build && npm link"
  },
  "bin": "./bin/temperature-data-statistics.js",
  "author": "jezzay",
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^22.1.3",
    "@types/node": "^9.4.6",
    "jest": "^22.3.0",
    "ts-jest": "^22.0.4",
    "tslib": "^1.9.0",
    "typescript": "^2.7.2"
  }
}
